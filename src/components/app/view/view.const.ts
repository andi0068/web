import type { ViewOptions } from './view.types';

export const BEM_CLASS = 'c-app__view';

export const DEFAULTS: Required<ViewOptions> = {
  scrollbar: 'default',
  size: 'full',
  border: 'none',
};
