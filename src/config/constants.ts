export const REDIRECT_PATHS = {
  public: '/',
  auth: '/login',
  user: '/dashboard',
};

export const STATE_CLIENT_CONFIG_KEY = 'state:client-config';
export const APP_CLIENT_CONFIG_KEY = 'app:client-config';
